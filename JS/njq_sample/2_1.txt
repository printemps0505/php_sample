・'と"で囲いが違うのは、単純に区別しているだけでどちらでもよい
・JSではプログラム実行時に２回読み込む。１回目は関数の有無の調査。２回目に上から順次実行。
・引用先がhttpsかhttpかわからない際は、あえて指定しないパターンもあるが、基本は指定する。
・関数(function 関数名())は、引数として文字列も入れることができるので、入れるタイミングを間違えると、うまく関数として機能しない。
・無名関数(funciton())は、①直で引数に入れる②変数に入れて使用する際に呼び出す、の２パターンがある
・変数に代入した無名関数を、あえて外に出して管理する方法がある（モジュールを利用）。これができれば
　格段に収入が上がるので、将来的に習得を目指す（黒本で学習）
